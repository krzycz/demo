name: triggered remotely

on:
  workflow_dispatch:
    inputs:
      param1:
        required: true
        type: string
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Build it
        run: |
          echo "ref: ${{ github.ref }}"
          echo "param1: ${{ inputs.param1 }}"
          echo "repo: ${{ github.repository.full_name }}"        
          echo "sender: ${{ github.sender.login }}"        
          echo "workflow: ${{ github.workflow }}"        
